#include "playerinfo.h"

PlayerInfo::PlayerInfo(Color playerColor, QString playerName, QString playerIcon) {
    this->playerColor = playerColor;
    this->playerName = playerName;
    this->playerIcon = playerIcon;
}
